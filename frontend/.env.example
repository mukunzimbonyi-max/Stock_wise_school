# Backend API base URL.
# - Leave unset (or set to empty) to use the Vite dev proxy (http://127.0.0.1:5000) in development.
# - In production builds set this to your Render backend URL, e.g.:
#   VITE_API_URL=https://stock-wise-school.onrender.com
VITE_API_URL=
